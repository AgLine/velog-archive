# 넥사크로에서 자주 사용했던것들

게시일: 2024-10-14T01:18:09.469Z
시리즈: No-Series

---

_작성중_
# Combo
#### 데이터 추가방법
![](https://velog.velcdn.com/images/agline/post/953003b7-a16b-4508-b77a-384288f4a468/image.png)

![](https://velog.velcdn.com/images/agline/post/c1578b60-be6d-4e24-b26c-676c20cb5b1c/image.png)

datacolumn: 사용자에게 보여지는이름
codecolumn: 백엔드에서 사용할코드
innerdataset: 콤보에서 사용할 dataset
#### 초기값 설정법
처음 combo의 index는 -1로 설정되어있음
index를 0으로 설정하면 해당 데이터셋의 첫번째 값을 보여줌
dataset의 첫번째에 초기값으로 설정하고싶은 값을 넣음
![](https://velog.velcdn.com/images/agline/post/8f0aa338-5cb9-4e20-8e60-8c3491e20401/image.png)
```
this.combo.set_index(0);
```

# Dataset
#### 데이터셋 row 추가
```
this.ds_dataset.addRow();
```
#### 추가한 row 값 넣기
```
var nRow = this.ds_dataset.addRow();
this.ds_dataset.setColumn(nRow,"columnName", data);
```
데이터셋이름.setColumn(행번호,"컬럼이름", 추가할데이터)
#### 데이터 가져오기
```
this.ds_dataset.getColumn(0,"columnName");
```
데이터셋이름.getColumn(행번호,"컬럼이름");
#### 데이터셋 필터적용
```
this.ds_dataset.set_enableevent(false);
	
// comboCode의 값이 'aa'로 시작하는 조건으로 필터 설정
this.ds_dataset.filter("comboCode.indexOf('aa') == 0");
	
this.ds_dataset.set_enableevent(true);
```
# Edit
``` 
//get
this.edtId.value;
//set
this.edtId.set_value();
```

# Transaction

```
var strId = "";
var strUrl = "svc::";
var inData = "";
var outData	= "";
var param = "";
var callBack = "";
this.transaction(strId, strUrl, inData, outData, param, callBack);

this.fn_callBack = function (svcID){

}
```
 
``` 
var strId = "svcid";
var strUrl = "svc::url";
// "백엔드에서받을이름=넥사크로 dataset"
var inData = "ds_input=ds_send";
//여러개일때 , 콤마로 구분하여 입력
var inData = "ds_input1=ds_send1, ds_input2=ds_send2";
//"넥사크로dataset=백엔드에서보내준이름"
var outData	= "ds_output=out_list";
var outData	= "resultMessage=resultMessage";
//여러개일때 , 콤마로 구분하여 입력
var outData	= "ds_output1=out_list1, ds_output2=out_list2";
//무조건 다음 파라미터를 입력할 때 공백한칸(스페이스바 한번누르기)
var param = "param1=1"+" param2=2";
var callBack = "fn_callback";
this.transaction(strId, strUrl, inData, outData, param, callBack);

this.fn_callBack = function (svcID){

}
```

여러개의 데이터들을 보낼 때 ", " 컴마+공백한칸 으로 해야만 백엔드에서 인식을 함
**ds_output1=out_list1, ds_output2=out_list2** << 이렇게

 ds_output1=out_list1,ds_output2=out_list2
 ds_output1=out_list1 ,ds_output2=out_list2
 이런식으로 보내면 데이터를 찾을 수 없다는 에러가 발생함
 
```
@RequestMapping("/test")
	public NexacroResult test( @ParamDataSet(name="ds_input") DataSet inputDs
								   ,@ParamVariable(name="inputVar") String inputVar){
	NexacroResult result = new NexacroResult();
    
    
    result.addDataSet("out_ds", list);
    result.addVariable("outVar", outVar);
    return result;
}
```

# 초기값을 넣어 새로운 폼 열기
디버깅을 하여 찾고싶은 값 바로밑에 trace("Break Point"); 작성
![](https://velog.velcdn.com/images/agline/post/53413a8d-bd9e-4095-a27f-a1e060cb8441/image.png)

![](https://velog.velcdn.com/images/agline/post/1c0b156f-e850-4d88-98fe-79ed60943288/image.png)

![](https://velog.velcdn.com/images/agline/post/b389de45-f5fb-46e5-bc50-f96437b9e3ef/image.png)

```
this.parent.parent.parent.arguments // boolean 초기값 존재 여부확인
this.parent.parent.parent.arguments.arrArg[0]
```