# 넥사크로에서 자주 사용했던것들

게시일: Mon, 14 Oct 2024 01:18:09 GMT
링크: https://velog.io/@agline/%EB%84%A5%EC%82%AC%ED%81%AC%EB%A1%9C%EC%97%90%EC%84%9C-%EC%9E%90%EC%A3%BC-%EC%82%AC%EC%9A%A9%ED%96%88%EB%8D%98%EA%B2%83%EB%93%A4

---

<p><em>작성중</em></p>
<h1 id="combo">Combo</h1>
<h4 id="데이터-추가방법">데이터 추가방법</h4>
<p><img alt="" src="https://velog.velcdn.com/images/agline/post/953003b7-a16b-4508-b77a-384288f4a468/image.png" /></p>
<p><img alt="" src="https://velog.velcdn.com/images/agline/post/c1578b60-be6d-4e24-b26c-676c20cb5b1c/image.png" /></p>
<p>datacolumn: 사용자에게 보여지는이름
codecolumn: 백엔드에서 사용할코드
innerdataset: 콤보에서 사용할 dataset</p>
<h4 id="초기값-설정법">초기값 설정법</h4>
<p>처음 combo의 index는 -1로 설정되어있음
index를 0으로 설정하면 해당 데이터셋의 첫번째 값을 보여줌
dataset의 첫번째에 초기값으로 설정하고싶은 값을 넣음
<img alt="" src="https://velog.velcdn.com/images/agline/post/8f0aa338-5cb9-4e20-8e60-8c3491e20401/image.png" /></p>
<pre><code>this.combo.set_index(0);</code></pre><h1 id="dataset">Dataset</h1>
<h4 id="데이터셋-row-추가">데이터셋 row 추가</h4>
<pre><code>this.ds_dataset.addRow();</code></pre><h4 id="추가한-row-값-넣기">추가한 row 값 넣기</h4>
<pre><code>var nRow = this.ds_dataset.addRow();
this.ds_dataset.setColumn(nRow,&quot;columnName&quot;, data);</code></pre><p>데이터셋이름.setColumn(행번호,&quot;컬럼이름&quot;, 추가할데이터)</p>
<h4 id="데이터-가져오기">데이터 가져오기</h4>
<pre><code>this.ds_dataset.getColumn(0,&quot;columnName&quot;);</code></pre><p>데이터셋이름.getColumn(행번호,&quot;컬럼이름&quot;);</p>
<h4 id="데이터셋-필터적용">데이터셋 필터적용</h4>
<pre><code>this.ds_dataset.set_enableevent(false);

// comboCode의 값이 'aa'로 시작하는 조건으로 필터 설정
this.ds_dataset.filter(&quot;comboCode.indexOf('aa') == 0&quot;);

this.ds_dataset.set_enableevent(true);</code></pre><h1 id="edit">Edit</h1>
<pre><code>//get
this.edtId.value;
//set
this.edtId.set_value();</code></pre><h1 id="transaction">Transaction</h1>
<pre><code>var strId = &quot;&quot;;
var strUrl = &quot;svc::&quot;;
var inData = &quot;&quot;;
var outData    = &quot;&quot;;
var param = &quot;&quot;;
var callBack = &quot;&quot;;
this.transaction(strId, strUrl, inData, outData, param, callBack);

this.fn_callBack = function (svcID){

}</code></pre><pre><code>var strId = &quot;svcid&quot;;
var strUrl = &quot;svc::url&quot;;
// &quot;백엔드에서받을이름=넥사크로 dataset&quot;
var inData = &quot;ds_input=ds_send&quot;;
//여러개일때 , 콤마로 구분하여 입력
var inData = &quot;ds_input1=ds_send1, ds_input2=ds_send2&quot;;
//&quot;넥사크로dataset=백엔드에서보내준이름&quot;
var outData    = &quot;ds_output=out_list&quot;;
var outData    = &quot;resultMessage=resultMessage&quot;;
//여러개일때 , 콤마로 구분하여 입력
var outData    = &quot;ds_output1=out_list1, ds_output2=out_list2&quot;;
//무조건 다음 파라미터를 입력할 때 공백한칸(스페이스바 한번누르기)
var param = &quot;param1=1&quot;+&quot; param2=2&quot;;
var callBack = &quot;fn_callback&quot;;
this.transaction(strId, strUrl, inData, outData, param, callBack);

this.fn_callBack = function (svcID){

}</code></pre><p>여러개의 데이터들을 보낼 때 &quot;, &quot; 컴마+공백한칸 으로 해야만 백엔드에서 인식을 함
<strong>ds_output1=out_list1, ds_output2=out_list2</strong> &lt;&lt; 이렇게</p>
<p> ds_output1=out_list1,ds_output2=out_list2
 ds_output1=out_list1 ,ds_output2=out_list2
 이런식으로 보내면 데이터를 찾을 수 없다는 에러가 발생함</p>
<pre><code>@RequestMapping(&quot;/test&quot;)
    public NexacroResult test( @ParamDataSet(name=&quot;ds_input&quot;) DataSet inputDs
                                   ,@ParamVariable(name=&quot;inputVar&quot;) String inputVar){
    NexacroResult result = new NexacroResult();


    result.addDataSet(&quot;out_ds&quot;, list);
    result.addVariable(&quot;outVar&quot;, outVar);
    return result;
}</code></pre><h1 id="초기값을-넣어-새로운-폼-열기">초기값을 넣어 새로운 폼 열기</h1>
<p>디버깅을 하여 찾고싶은 값 바로밑에 trace(&quot;Break Point&quot;); 작성
<img alt="" src="https://velog.velcdn.com/images/agline/post/53413a8d-bd9e-4095-a27f-a1e060cb8441/image.png" /></p>
<p><img alt="" src="https://velog.velcdn.com/images/agline/post/1c0b156f-e850-4d88-98fe-79ed60943288/image.png" /></p>
<p><img alt="" src="https://velog.velcdn.com/images/agline/post/b389de45-f5fb-46e5-bc50-f96437b9e3ef/image.png" /></p>
<pre><code>this.parent.parent.parent.arguments // boolean 초기값 존재 여부확인
this.parent.parent.parent.arguments.arrArg[0]</code></pre>